---
import { withBase } from '../lib/url';

const path = Astro.url.pathname.replace(/\/$/, '');
const isHome = path === withBase('/').replace(/\/$/, '');
---

<footer class:list={['ui', { centered: isHome }]}>
  <div class="inner">
    <p>
      Independent study notes on <em>Designing Data-Intensive Applications</em>, 2nd edition, by
      Martin Kleppmann &amp; Chris Riccomini. Not affiliated with the authors or O&rsquo;Reilly —
      <a href="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781098119058/"
        >buy the book</a
      >; it&rsquo;s worth every page.
    </p>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--border);
    margin-top: var(--space-9);
  }

  .inner {
    max-width: 76rem;
    margin-inline: auto;
    padding: var(--space-6) var(--space-4);
    color: var(--text-faint);
    font-size: var(--text-sm);
  }

  p {
    max-width: 60ch;
  }

  footer.centered .inner {
    text-align: center;
  }

  footer.centered p {
    margin-inline: auto;
  }
</style>
